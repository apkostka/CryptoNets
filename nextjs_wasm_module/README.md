## Demo for Private Identity WASM

Add your API token and Private Identity API URL in `.env`, using `.env.example` as a guide.

Install dependencies (i.e. `yarn install`) and then run `yarn dev` to start the application on http://localhost:3000.